# Log: Workshop Plan

**Created:** 2026-05-16 17:25 UTC

## Prompt

User asked for a detailed plan for a three-day workshop (18 one-hour sessions)
introducing data science and empirical study methods to the intended audience
(second-year undergraduates in CS/SE). Required use of `.bib` files in
`$HOME/bibliography/bib` for inspiration. Must be hands-on and practical.
Output to `./plan.txt` in Markdown format.

## Actions Taken

1. Ran `brief .` to get project overview.
2. Read `README.md` and all current lesson files (`intro`, `dataframes`, `size`,
   `hypotest`, `finale`).
3. Read all unused lesson index files:
   `unused/01_intro`, `unused/02_basic`, `unused/03_robust`, `unused/04_hypotest`,
   `unused/05_largenum`, `unused/06_nonpar`, `unused/correlation`,
   `unused/curve-fitting`, `unused/gini`, `unused/measuring-contributions`,
   `unused/programmer-survey`, `unused/survival-analysis`,
   `unused/other-peoples-data`, `unused/parameter-estimation`,
   `unused/capacity-planning`, `unused/language-popularity`, `unused/theory`.
4. Sampled ~40 `.bib` files in `$HOME/bibliography/bib/` to identify relevant
   empirical SE papers.
5. Read `unused/extras/bib.bib` and `unused/extras/proposal.txt` for
   additional context.
6. Created `log/` directory.
7. Wrote `plan.txt` as an 18-session workshop plan.

## Key Decisions

- Structured as Day 1 (tools and data), Day 2 (statistical inference),
  Day 3 (repository mining and AI impact).
- Every session has a motivating question drawn from a real study.
- Sessions 6, 12, and 18 are pure labs with no new content.
- Included pacing notes with explicit fallback options if time runs short.
- All datasets traceable to existing material in the `unused/` directory or
  freely available public sources.
- Used Polars and Altair throughout (matching project dependencies).
- Dropped pandas and scipy from the tool stack where possible; used scipy only
  for statistical tests that Polars does not implement.
- Included the emerging AI-in-SE literature (Fucci, Fu, El Haji, Noda) in
  Session 16 because the audience uses LLMs regularly.
